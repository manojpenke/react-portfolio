import React, { ReactNode } from 'react';
import './style.scss';
export interface PropsInputType {
    className?: string;
    label?: ReactNode;
    value?: string;
    placeholder?: string;
    disabled?: boolean;
    maxLen?: number;
    type?: 'textarea' | 'text' | 'number' | 'password' | 'tel';
    rows?: number;
    theme?: string;
    showClear?: boolean;
    onChange?: Function;
}
declare const ComponentView: ({ className, label, value, placeholder, disabled, maxLen, type, rows, theme, showClear, onChange, }: PropsInputType) => React.JSX.Element;
export default ComponentView;
