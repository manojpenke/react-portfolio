import React from 'react';
import './style.scss';
export interface PropsPreviewType {
    className?: string;
    show?: boolean;
    start?: number;
    images: any[];
    onClose?: Function;
    onChange?: Function;
}
declare const ComponentView: ({ className, show, start, images, onClose, onChange, }: PropsPreviewType) => React.JSX.Element;
export default ComponentView;
