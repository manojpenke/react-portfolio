export interface PropsImagePreviewType {
    images: any[];
    start?: number;
    type?: 'private' | 'public';
}
declare const ComponentView: ({ images, start, type, }: PropsImagePreviewType) => void;
export default ComponentView;
