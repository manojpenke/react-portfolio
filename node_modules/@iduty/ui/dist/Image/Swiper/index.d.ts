import React from 'react';
import './style.scss';
export interface PropsSwiperType {
    className?: string;
    images: any[];
    auto?: boolean;
    safe?: number;
    duration?: number;
    bar?: boolean;
}
declare const ComponentView: ({ className, images, auto, safe, duration, bar, }: PropsSwiperType) => React.JSX.Element | null;
export default ComponentView;
