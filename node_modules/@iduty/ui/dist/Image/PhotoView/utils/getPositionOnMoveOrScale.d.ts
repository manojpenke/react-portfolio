export default function getPositionOnMoveOrScale(x: number, y: number, width: number, height: number, scale: number, toScale: number, clientX: number | undefined, clientY: number | undefined, container: any, offsetX?: number, offsetY?: number): {
    x: number;
    y: number;
    lastCX: number;
    lastCY: number;
};
