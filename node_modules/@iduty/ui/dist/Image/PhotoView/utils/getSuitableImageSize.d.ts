export default function getSuitableImageSize(naturalWidth: number, naturalHeight: number, rotate: number, container: any): {
    width: number;
    height: number;
    x: number;
    y: number;
    pause: boolean;
};
