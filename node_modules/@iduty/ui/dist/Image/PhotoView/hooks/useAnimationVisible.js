"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const react_1 = require("react");
const useForkedVariable_1 = __importDefault(require("./useForkedVariable"));
function useAnimationVisible(visible, afterClose) {
    const [, handleRender] = (0, react_1.useReducer)((c) => !c, false);
    const activeAnimation = (0, react_1.useRef)(0);
    const [realVisible, modifyRealVisible] = (0, useForkedVariable_1.default)(visible, (modify) => {
        if (visible) {
            modify(visible);
            activeAnimation.current = 1;
        }
        else {
            activeAnimation.current = 2;
        }
    });
    function onAnimationEnd() {
        handleRender();
        if (activeAnimation.current === 2) {
            modifyRealVisible(false);
            if (afterClose) {
                afterClose();
            }
        }
        activeAnimation.current = 0;
    }
    return [
        realVisible,
        activeAnimation.current,
        onAnimationEnd,
    ];
}
exports.default = useAnimationVisible;
