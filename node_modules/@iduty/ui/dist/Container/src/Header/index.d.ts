import React, { ReactNode } from 'react';
import './style.scss';
export interface PropsHeaderType {
    show?: boolean;
    type?: string;
    pageTitle?: ReactNode;
}
declare const ComponentView: ({ show, type, pageTitle, }: PropsHeaderType) => React.JSX.Element | null;
export default ComponentView;
