import React, { ReactNode } from 'react';
import './style.scss';
export interface PropsFooterType {
    children?: ReactNode;
    className?: string;
}
declare const ComponentView: ({ children, className, }: PropsFooterType) => React.JSX.Element;
export default ComponentView;
