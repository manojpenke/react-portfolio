import React, { ReactNode } from 'react';
import './style.scss';
export interface PropsListType {
    children?: ReactNode;
    initialLoad?: boolean;
    pageStart?: number;
    onLoad: Function;
    hasMore: boolean;
    getParent?: any;
    threshold?: number;
    useWindow?: boolean;
    loader?: ReactNode;
    noMore?: ReactNode;
}
declare const ComponentView: ({ children, initialLoad, pageStart, onLoad, hasMore, getParent, threshold, useWindow, loader, noMore }: PropsListType) => React.JSX.Element;
export default ComponentView;
