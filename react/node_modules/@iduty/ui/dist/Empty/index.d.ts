import React, { ReactNode } from 'react';
import './style.scss';
export interface PropsEmptyType {
    type?: string;
    title?: string;
    message?: ReactNode;
    onEvent?: Function;
    screen?: boolean;
    btn?: string;
    themesWrap?: any;
    themesImg?: any;
}
declare const ComponentView: ({ type, title, message, onEvent, screen, btn, themesWrap, themesImg, }: PropsEmptyType) => React.JSX.Element;
export default ComponentView;
