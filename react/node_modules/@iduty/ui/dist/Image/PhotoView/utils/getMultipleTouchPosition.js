"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
function getMultipleTouchPosition(evt) {
    const { clientX, clientY } = evt.touches[0];
    if (evt.touches.length >= 2) {
        const { clientX: nextClientX, clientY: nextClientY } = evt.touches[1];
        return [
            (clientX + nextClientX) / 2,
            (clientY + nextClientY) / 2,
            Math.sqrt(Math.pow((nextClientX - clientX), 2) + Math.pow((nextClientY - clientY), 2)),
        ];
    }
    return [clientX, clientY, 0];
}
exports.default = getMultipleTouchPosition;
