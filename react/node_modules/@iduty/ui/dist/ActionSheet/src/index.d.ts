import React, { ReactNode } from 'react';
import './style.scss';
export interface PropsActionType {
    className?: string;
    title?: ReactNode;
    actions: any[];
    cancelText?: string;
    onClose?: Function;
}
declare const ComponentView: ({ className, title, actions, cancelText, onClose, }: PropsActionType) => React.ReactPortal;
export default ComponentView;
