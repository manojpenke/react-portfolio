import React from 'react';
import './style.scss';
export interface PropsMaskType {
    onClick?: Function;
    show?: boolean;
}
declare const ComponentView: ({ onClick, show, }: PropsMaskType) => React.JSX.Element;
export default ComponentView;
