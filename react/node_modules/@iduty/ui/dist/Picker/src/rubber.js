"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.rubberbandIfOutOfBounds = exports.rubberband = exports.bound = void 0;
function bound(position, min, max) {
    let ret = position;
    if (min !== undefined) {
        ret = Math.max(position, min);
    }
    if (max !== undefined) {
        ret = Math.min(ret, max);
    }
    return ret;
}
exports.bound = bound;
function rubberband(distance, dimension, constant) {
    const res = (distance * dimension * constant) / (dimension + constant * distance);
    return res;
}
exports.rubberband = rubberband;
function rubberbandIfOutOfBounds(position, min, max, dimension, constant = 0.15) {
    if (constant === 0)
        return bound(position, min, max);
    if (position < min)
        return -rubberband(min - position, dimension, constant) + min;
    if (position > max)
        return +rubberband(position - max, dimension, constant) + max;
    return position;
}
exports.rubberbandIfOutOfBounds = rubberbandIfOutOfBounds;
