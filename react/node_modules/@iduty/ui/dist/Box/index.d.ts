import React from 'react';
import './style.scss';
export interface PropsBoxType {
    type: string;
    title?: React.ReactNode;
    children?: React.ReactNode;
    show?: boolean;
    className?: string;
    submitBtn?: React.ReactNode;
    cancelBtn?: React.ReactNode;
    onCancel?: Function;
    onSubmit?: Function;
}
declare const ComponentView: ({ type, title, children, show, className, submitBtn, cancelBtn, onCancel, onSubmit, }: PropsBoxType) => React.ReactPortal | null;
export default ComponentView;
