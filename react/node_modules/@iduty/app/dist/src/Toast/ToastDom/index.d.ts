import React from 'react';
import './style.scss';
export interface PropsToastType {
    show?: boolean;
    message?: React.ReactNode;
    duration?: number;
    className?: string;
}
declare const ComponentView: ({ show, message, duration, className, }: PropsToastType) => React.ReactPortal | null;
export default ComponentView;
